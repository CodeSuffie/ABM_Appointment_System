namespace Repositories;

public class ModelRepository
{
    public async Task<TimeSpan> GetTimeAsync(CancellationToken cancellationToken)
    {
        // TODO: Get model time
        return new TimeSpan(0, 0, 0);
    }
}